// Core functionalities for crew-ai-ts

/**
 * Defines the execution process for a Crew.
 */
export enum CrewProcess {
	SEQUENTIAL = "sequential",
	HIERARCHICAL = "hierarchical", // Placeholder for a potential future process
}

// Re-export other core utilities, types, or classes as they are added.
